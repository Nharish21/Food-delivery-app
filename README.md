# Food-delivery-app
"A robust Food Delivery REST API backend built with Java 17 and Spring Boot. Follows a clean 3-tier MVC architecture featuring Spring Data JPA and MySQL integration. Implements input validations, Lombok models, custom data query repositories, transactional integrity, and optimized relational endpoints for restaurants, menus, and user orders."
